import 'package:flutter/material.dart';


class showPopUpGelirUpdate extends StatelessWidget {
  const showPopUpGelirUpdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
